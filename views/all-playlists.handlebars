<!-- Loop over each object in the projects array -->
{{#each playlists as |playlist| }}
<div class="row">
  <div class="col-md-5">
    <h2>
      <a href="/playlist/{{playlist.id}}">{{playlist.title}}</a>
    </h2>
    <p>Created by {{playlist.user.username}} on {{format_date playlist.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{#each playlist.animes as |anime|}}
      {{anime.anime_title}}

      {{/each}}
    </p>
    <a class="btn-floating" data-id="{{playlist.id}}"><i class="material-icons">favorite_border</i></a><span id="favorites-number">{{playlist.favoritesCount}}</span>
  </div>
</div>
{{/each}}
